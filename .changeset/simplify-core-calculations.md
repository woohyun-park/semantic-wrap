---
"@semantic-wrap/core": patch
---

Select greedy break candidates in one scan without sorting or searching the candidate list again. Consolidate immutable array helpers while preserving layout results and frozen snapshots.
